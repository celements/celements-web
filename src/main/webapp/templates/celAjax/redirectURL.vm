## request url has to conform to HttpServletResponse.sendRedirect(String), see javadoc
#set($url = $request.url)
#if("$!url" == "")
   #set($url = $doc.getURL())
#end
<p><b>url: </b>$url</p>
<p><b>url fixUmlHtmlEncodings: </b>$services.celementsweb.convertToPlainText($url)</p>
<p><b>url utf8: </b>$services.celementsweb.encodeUrlToUtf8($url)</p>

## $response.sendRedirect($url)
