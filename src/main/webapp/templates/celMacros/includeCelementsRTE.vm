#if("$!lazyAdd" == '')
  #set($lazyAdd = true)
#end
#set($jsService = $services.javascript)
#set($jsLoadModeDefer = $!jsService.getJsLoadMode("DEFER"))
#set($extJsParamDefer = $!jsService.createExtJSParam().setAction('file').setLoadMode($jsLoadModeDefer).setLazyLoad($lazyAdd))
#foreach($jsFile in $services.rteconfig.rteImplementation.javaScriptFiles)
  $!jsService.includeExtJsFile($extJsParamDefer.setJsFile($jsFile))
#end
